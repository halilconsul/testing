import AppConstants from '../constants/AppConstants.js';

const initialState = {
    auth: false
};

export default function (state=initialState, action) {
    switch (action.type) {

    }
    return state;
}